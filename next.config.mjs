/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', "avatars.githubusercontent.com", "avatars.slack-edge.com"],
    },
};

export default nextConfig;
